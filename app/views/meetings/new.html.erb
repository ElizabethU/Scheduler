<div class='col-lg-3'></div>
  <div class='signup-form col-lg-6'>
    <h1>Schedule Meeting</h1>

    <% if current_user && current_user.admin && current_user.execs.count > 0 %>
      <%= render 'form' %>

    <% else %>
      You must be logged in to schedule a meeting!
    <% end %>
  <br>
  <br>

  </div>
<div class='col-lg-3'></div>

