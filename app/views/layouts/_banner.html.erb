<div class='navbar'>
  <ul class='nav-main'>
    <div class='nav-div icon'>
      <%= link_to(image_tag("emc-isilon-logo.jpg", :alt => "EMC Isilon logo", size:'100x100', :title => "Click here to return to Home"), "/") %>
    </div>
    <div class='nav-div links'>
      <% if current_user %>
        <li>
          <span class='current-user'>Signed in as <%= link_to "#{current_user.name}", user_path(current_user) %></span>
          <%= link_to "Sign out", "/logout", class: 'btn btn-default' %>
        </li>
      <% else %>
        <li class='login open-modal'><%= link_to "Log In", "/login", class: 'btn btn-default'%></li>
      <% end %>
    </div>
  </ul>
</div>